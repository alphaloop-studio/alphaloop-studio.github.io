(async()=>{try{if(!('DecompressionStream'in window))throw new Error('이 브라우저는 압축 실행을 지원하지 않습니다. 최신 Safari 또는 Chrome을 사용하십시오.');const s=atob(window.__EC_PAYLOAD||'');const a=new Uint8Array(s.length);for(let i=0;i<s.length;i++)a[i]=s.charCodeAt(i);const stream=new Blob([a]).stream().pipeThrough(new DecompressionStream('gzip'));const html=await new Response(stream).text();document.open();document.write(html);document.close();}catch(e){document.getElementById('boot').innerHTML='<b>실행 환경 오류</b><small>'+e.message+'</small>';console.error(e);}})();
