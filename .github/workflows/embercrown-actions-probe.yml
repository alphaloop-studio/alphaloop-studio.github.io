name: EMBERCROWN Actions Probe
on:
  workflow_dispatch:
  push:
    branches: [master]
    paths:
      - "mobile-prototypes/embercrown-cinder-oath-forged/source/probe-trigger.txt"
permissions:
  contents: read
jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Actions execution is available"
